# Input

The introduction claims robust generalization, but the artifact bundle has a single benchmark result and no held-out shift test.

# Expected behavior

Emit a blocker with artifact locator, require narrowed language or a robustness experiment, and return `NOT_READY`.
