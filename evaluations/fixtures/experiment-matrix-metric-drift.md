# Input

The main metric changes from exact match to a custom judge score after early results are weak.

# Expected behavior

Preserve the original metric, create a new matrix version, label the new analysis exploratory, and require renewed approval before execution.
