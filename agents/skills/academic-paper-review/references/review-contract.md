# Review contract

Write findings as `severity`, `evidence`, `impact`, `requested remedy`, and `confidence`. Use `blocking`, `major`, `minor`, or `question`; severity measures the claim's importance, not reviewer certainty.

For numerical checks, compare denominator, split, seed/aggregation, metric definition, and matched setting before calling results inconsistent. Preserve an explicit `review_limitations` section.
