# Research-loop contract

Each hypothesis includes ID, prediction, competing hypotheses, baseline, metric, falsifier, evidence links, estimated cost, status, and revision history. Each run includes immutable planned prediction, revision, config, data ID, seeds, execution status, raw artifact links, result classification, and next decision.

Do not delete rejected hypotheses or failed runs. Create a successor hypothesis instead of modifying historical text.
