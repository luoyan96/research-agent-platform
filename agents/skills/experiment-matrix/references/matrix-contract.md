# Experiment matrix contract

Each row contains `claim_id`, `question`, `run_id`, priority, baseline, intervention, data ID/split, metric and aggregation, seeds, expected C0 outcome, expected alternative outcome, cost, stop rule, dependencies, and artifact paths.

Keep one canonical metrics definition. Any later change to metric, split, baseline, or primary endpoint receives a new matrix version and renewed approval.
